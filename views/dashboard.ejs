<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - BVAR19 Portal</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>BVAR19 Portal</h1>
      <nav>
        <a href="/">Home</a>
        <% if (user.isAdmin) { %>
          <a href="/admin">Admin Panel</a>
        <% } %>
        <a href="/logout">Logout</a>
      </nav>
    </header>
    
    <main>
      <div class="dashboard">
        <div class="welcome-banner">
          <h2>Welcome, <%= user.firstName %>!</h2>
          <p>You are logged in to the BVAR19 Portal.</p>
        </div>
        
        <div class="user-info">
          <h3>Your Account Information</h3>
          <ul>
            <li><strong>Name:</strong> <%= user.displayName %></li>
            <li><strong>Email:</strong> <%= user.email %></li>
            <li><strong>Role<%= user.roles && user.roles.length > 1 ? 's' : '' %>:</strong> 
              <% if (user.isAdmin) { %>
                Administrator
                <% if (user.roles && user.roles.length > 0) { %> + <% } %>
              <% } %>
              <% if (user.roles && user.roles.length > 0) { %>
                <%= user.roles.join(', ') %>
              <% } %>
            </li>
            <li><strong>Joined:</strong> <%= new Date(user.createdAt).toLocaleDateString() %></li>
          </ul>
        </div>
        
        <div class="portal-content">
          <h3>Training Management</h3>
          
          <div class="dashboard-cards">
            <% if (user.roles && user.roles.includes('Student')) { %>
              <div class="card">
                <h4>Student Actions</h4>
                <ul>
                  <li><a href="/training/submit">Submit Training Certificate</a></li>
                  <li><a href="/training/my-submissions">View My Submissions</a></li>
                </ul>
              </div>
            <% } %>
            
            <% if (user.isAdmin || (user.roles && user.roles.includes('Approver')) || (user.roles && user.roles.includes('Training Officer'))) { %>
              <div class="card">
                <h4>Approver Actions</h4>
                <ul>
                  <li><a href="/training/approver/dashboard">Review Submissions</a></li>
                </ul>
              </div>
            <% } %>
            
            <% if (user.isAdmin || (user.roles && user.roles.includes('Training Officer'))) { %>
              <div class="card">
                <h4>Training Officer Actions</h4>
                <ul>
                  <li><a href="/training/manage-classes">Manage Training Classes</a></li>
                </ul>
              </div>
            <% } %>
            
            <div class="card">
              <h4>Quick Links</h4>
              <ul>
                <li><a href="#">My Profile</a></li>
                <li><a href="#">Settings</a></li>
                <li><a href="#">Help Center</a></li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </main>
    
    <footer>
      <p>&copy; <%= new Date().getFullYear() %> BVAR19.org. All rights reserved.</p>
    </footer>
  </div>
</body>
</html> 