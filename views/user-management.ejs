<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Panel - Training Database</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('./partials/header', { user }) %>
  
  <div class="container mt-4">
    <div class="row mb-4">
      <div class="col">
        <h1>Admin Panel</h1>
        <p>Manage users and their access privileges. Need to add a better admin panel</p>
        <a href="/demo-portal" class="btn btn-info">
          <i class="fas fa-flask"></i> Demo Portal
        </a>
        <a href="/training/admin/members" class="btn btn-primary ml-2">
          <i class="fas fa-users-cog"></i> Member Certificates
        </a>
      </div>
    </div>
    
    <% if (locals.error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    
    <% if (locals.success) { %>
      <div class="alert alert-success"><%= success %></div>
    <% } %>

    <div class="card mb-4">
      <div class="card-header bg-secondary text-white">
        <h5 class="mb-0">Add Users</h5>
      </div>
      <div class="card-body">
        <div class="row">
          <div class="col-md-6">
            <h6>Manual Entry</h6>
            <form action="/admin/add-user" method="POST">
              <div class="form-group">
                <label for="add-email">Email</label>
                <input type="email" class="form-control" id="add-email" name="email" required>
              </div>
              <div class="form-row">
                <div class="form-group col-md-6">
                  <label for="add-first-name">First Name</label>
                  <input type="text" class="form-control" id="add-first-name" name="firstName">
                </div>
                <div class="form-group col-md-6">
                  <label for="add-last-name">Last Name</label>
                  <input type="text" class="form-control" id="add-last-name" name="lastName">
                </div>
              </div>
              <div class="form-group">
                <label for="add-display-name">Display Name</label>
                <input type="text" class="form-control" id="add-display-name" name="displayName" placeholder="Optional">
              </div>
              <div class="form-group">
                <label>Roles</label>
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="add-role-student" name="roles" value="Student" checked>
                  <label class="custom-control-label" for="add-role-student">Student</label>
                </div>
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="add-role-approver" name="roles" value="Approver">
                  <label class="custom-control-label" for="add-role-approver">Approver</label>
                </div>
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="add-role-officer" name="roles" value="Training Officer">
                  <label class="custom-control-label" for="add-role-officer">Training Officer</label>
                </div>
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="add-role-rescue-officer" name="roles" value="Rescue Officer">
                  <label class="custom-control-label" for="add-role-rescue-officer">Rescue Officer</label>
                </div>
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="add-role-evaluator" name="roles" value="Evaluator">
                  <label class="custom-control-label" for="add-role-evaluator">Evaluator</label>
                </div>
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="add-role-rescue-chief" name="roles" value="Rescue Chief">
                  <label class="custom-control-label" for="add-role-rescue-chief">Rescue Chief</label>
                </div>
              </div>
              <div class="custom-control custom-checkbox mb-3">
                <input type="checkbox" class="custom-control-input" id="add-is-admin" name="isAdmin">
                <label class="custom-control-label" for="add-is-admin">Admin</label>
              </div>
              <button type="submit" class="btn btn-primary">Add User</button>
            </form>
          </div>
          <div class="col-md-6">
            <h6>Import (CSV)</h6>
            <p class="text-muted mb-2">Headers: email, displayName, firstName, lastName, roles, isAdmin</p>
            <form action="/admin/import-users" method="POST" enctype="multipart/form-data">
              <div class="form-group">
                <input type="file" class="form-control-file" name="csvFile" accept=".csv" required>
              </div>
              <button type="submit" class="btn btn-outline-primary">Import CSV</button>
            </form>
          </div>
        </div>
      </div>
    </div>
    
    <div class="card">
      <div class="card-header bg-primary text-white">
        <h5 class="mb-0">User Management</h5>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Joined</th>
                <th>Admin Status</th>
                <th>Roles</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% users.forEach(function(userItem) { %>
                <tr>
                  <td><%= userItem.displayName %></td>
                  <td><%= userItem.email %></td>
                  <td><%= new Date(userItem.createdAt).toLocaleDateString() %></td>
                  <td><span class="badge badge-<%= userItem.isAdmin ? 'success' : 'secondary' %>">
                    <%= userItem.isAdmin ? 'Admin' : 'User' %>
                  </span></td>
                  <td>
                    <form action="/admin/update-user/<%= userItem._id %>" method="POST">
                      <div class="form-group mb-0">
                        <div class="custom-control custom-checkbox">
                          <input type="checkbox" class="custom-control-input" id="role-student-<%= userItem._id %>" 
                            name="roles" value="Student" 
                            <%= userItem.roles && userItem.roles.includes('Student') ? 'checked' : '' %>>
                          <label class="custom-control-label" for="role-student-<%= userItem._id %>">Student</label>
                        </div>
                        <div class="custom-control custom-checkbox">
                          <input type="checkbox" class="custom-control-input" id="role-approver-<%= userItem._id %>" 
                            name="roles" value="Approver" 
                            <%= userItem.roles && userItem.roles.includes('Approver') ? 'checked' : '' %>>
                          <label class="custom-control-label" for="role-approver-<%= userItem._id %>">Approver</label>
                        </div>
                        <div class="custom-control custom-checkbox">
                          <input type="checkbox" class="custom-control-input" id="role-officer-<%= userItem._id %>" 
                            name="roles" value="Training Officer" 
                            <%= userItem.roles && userItem.roles.includes('Training Officer') ? 'checked' : '' %>>
                          <label class="custom-control-label" for="role-officer-<%= userItem._id %>">Training Officer</label>
                        </div>
                        <div class="custom-control custom-checkbox">
                          <input type="checkbox" class="custom-control-input" id="role-rescue-officer-<%= userItem._id %>" 
                            name="roles" value="Rescue Officer" 
                            <%= userItem.roles && userItem.roles.includes('Rescue Officer') ? 'checked' : '' %>>
                          <label class="custom-control-label" for="role-rescue-officer-<%= userItem._id %>">Rescue Officer</label>
                        </div>
                        <div class="custom-control custom-checkbox">
                          <input type="checkbox" class="custom-control-input" id="role-evaluator-<%= userItem._id %>" 
                            name="roles" value="Evaluator" 
                            <%= userItem.roles && userItem.roles.includes('Evaluator') ? 'checked' : '' %>>
                          <label class="custom-control-label" for="role-evaluator-<%= userItem._id %>">Evaluator</label>
                        </div>
                        <div class="custom-control custom-checkbox">
                          <input type="checkbox" class="custom-control-input" id="role-rescue-chief-<%= userItem._id %>" 
                            name="roles" value="Rescue Chief" 
                            <%= userItem.roles && userItem.roles.includes('Rescue Chief') ? 'checked' : '' %>>
                          <label class="custom-control-label" for="role-rescue-chief-<%= userItem._id %>">Rescue Chief</label>
                        </div>
                      </div>
                  </td>
                  <td>
                    <button type="submit" class="btn btn-sm btn-primary">
                      <i class="fas fa-save"></i> Save Roles
                    </button>
                    </form>
                    <button type="button" 
                            class="btn btn-sm btn-outline-secondary mt-2 edit-user-btn"
                            data-toggle="modal"
                            data-target="#editUserModal"
                            data-user='<%- JSON.stringify({
                              id: userItem._id,
                              firstName: userItem.firstName || '',
                              middleName: userItem.middleName || '',
                              lastName: userItem.lastName || '',
                              displayName: userItem.displayName || '',
                              email: userItem.email || ''
                            }) %>'>
                      <i class="fas fa-user-edit"></i> Edit Details
                    </button>
                    <% if (userItem.email !== 'adavis@bvar19.org' || !userItem.isAdmin) { %>
                      <form action="/admin/toggle-admin/<%= userItem._id %>" method="POST" class="mt-2">
                        <button type="submit" class="btn btn-sm btn-<%= userItem.isAdmin ? 'warning' : 'success' %>">
                          <i class="fas <%= userItem.isAdmin ? 'fa-user-minus' : 'fa-user-shield' %>"></i> 
                          <%= userItem.isAdmin ? 'Remove Admin' : 'Make Admin' %>
                        </button>
                      </form>
                    <% } %>
                    <% if (userItem.email !== 'adavis@bvar19.org') { %>
                      <form action="/admin/delete-user/<%= userItem._id %>" method="POST" class="mt-2" onsubmit="return confirm('Delete this user?');">
                        <button type="submit" class="btn btn-sm btn-danger">
                          <i class="fas fa-trash"></i> Delete User
                        </button>
                      </form>
                    <% } %>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  
  <div class="modal fade" id="editUserModal" tabindex="-1" role="dialog" aria-labelledby="editUserModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editUserModalLabel">Edit User Details</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <form id="editUserForm" method="POST">
          <div class="modal-body">
            <div class="form-group">
              <label for="editUserFirstName">First Name</label>
              <input type="text" class="form-control" id="editUserFirstName" name="firstName" required>
            </div>
            <div class="form-group">
              <label for="editUserMiddleName">Middle Name</label>
              <input type="text" class="form-control" id="editUserMiddleName" name="middleName">
            </div>
            <div class="form-group">
              <label for="editUserLastName">Last Name</label>
              <input type="text" class="form-control" id="editUserLastName" name="lastName" required>
            </div>
            <div class="form-group">
              <label for="editUserDisplayName">Display Name</label>
              <input type="text" class="form-control" id="editUserDisplayName" name="displayName">
              <small class="form-text text-muted">Leave blank to use "First [Middle] Last".</small>
            </div>
            <div class="form-group">
              <label>Email</label>
              <input type="text" class="form-control" id="editUserEmail" readonly>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-primary">Save Changes</button>
          </div>
        </form>
      </div>
    </div>
  </div>
  
  <%- include('./partials/footer') %>
  <%- include('./partials/scripts') %>
  <script>
    $(function() {
      $('#editUserModal').on('show.bs.modal', function(event) {
        const button = $(event.relatedTarget);
        let userData = button.attr('data-user');
        try {
          userData = userData ? JSON.parse(userData) : null;
        } catch (err) {
          userData = null;
        }

        if (!userData) {
          return;
        }

        const form = $('#editUserForm');
        form.attr('action', `/admin/update-user-details/${userData.id}`);
        $('#editUserFirstName').val(userData.firstName || '');
        $('#editUserMiddleName').val(userData.middleName || '');
        $('#editUserLastName').val(userData.lastName || '');
        $('#editUserDisplayName').val(userData.displayName || '');
        $('#editUserEmail').val(userData.email || '');
      });
    });
  </script>
</body>
</html> 